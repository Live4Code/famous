{
  "name": "famous",
  "description": "A starter project to learn Famo.us",
  "version": "0.0.0",
  "engines": {
    "node": ">= 0.10.0"
  },
  "devDependencies": {
    "http-server": "^0.6.1"
  },
  "scripts": {
    "postinstall": "bower install --allow-root",
    "prestart": "npm install",
    "start": "http-server --cors -a 0.0.0.0 -p 8000"
  }
}
